import React, { Fragment } from 'react';
import { Content } from '../components/homepage/Content';

const whois = () => {
  return (
    <Fragment>
      <Content />
    </Fragment>
  );
};

export default whois;
